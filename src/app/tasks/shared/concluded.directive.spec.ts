import { ConcludedDirective } from './concluded.directive';

describe('ConcludedDirective', () => {
  it('should create an instance', () => {
    const directive = new ConcludedDirective();
    expect(directive).toBeTruthy();
  });
});
